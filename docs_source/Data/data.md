# Data

## Sections to fill

- Synthetic data only: generated by FDM solver (`FracSDOF`, `src/fpinns/fdm.py`)
- Parameter choices: m, k, c, x0, v0, alpha_actual, tau_actual, T, dt, force magnitude/frequency
- Observation points (t_obs) vs physics collocation points (t_phy)
- Noise levels, seeds
- Train/validation split if any
- Ground truth figures from `prev_codes/`