# Research Questions

## Sections to fill

1. Can the PINN recover alpha (fractional order) accurately from noisy observations?
2. Can the PINN recover tau (memory timescale) accurately?
3. Is joint estimation of alpha and tau stable, or do they conflate?
4. How sensitive is recovery to initial guess (alpha_init, tau_init)?
5. How robust is recovery to observation noise, dt, and force frequency?
6. Do design choices (batch norm, mask, activation init) help?